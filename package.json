{
  "name": "ai_control_panel",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "postinstall": "npm install --prefix client && npm install --prefix server && npm install --prefix metaclient",
    "start": "npm start --prefix server",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "metaclient": "npm run dev --prefix metaclient",
    "dev": "concurrently \"npm run server\" \"npm run client\" \"npm run metaclient\"",
    "build": "npm run build --prefix client"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.6.0",
    "nodemon": "^2.0.21"
  }
}
